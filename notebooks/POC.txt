I want to build a Python proof of concept that converts exported PathWise HTML model documentation into a machine-readable dependency graph.

## Background

The PathWise model documentation contains a hierarchical structure similar to:

* Model

  * Data Sources

    * market_data
    * inforce
    * params
    * actuarial assumptions
    * etc.
  * Model Libraries

    * Library

      * Inputs
      * Calculations

        * Dictionary
        * Computation
        * Projection
      * Outputs
      * User-defined functions
  * Project-level Calculations

    * Dictionary
    * Computation
    * Projection
  * Project-level User-defined functions
  * Calculation Results

Inputs, calculations, projections, and outputs contain expressions that reference other model objects.

Examples of relationships may look conceptually like:

`N_HYBRID_INDEX = Constants.N_HYBRID_INDEX`

or

`forward_rate = Projection.revised_short_rate`

or a formula may reference several variables and/or call a user-defined function.

The exported HTML also appears to contain hyperlinks between referenced model objects. If hyperlinks contain resolved target information, prefer using the hyperlink target instead of trying to infer the target from text.

## Goal

Build a Python parser that automatically extracts:

1. model components/nodes
2. direct dependency relationships/edges
3. variable aliases / mappings where possible
4. downstream dependency impact using graph traversal

Do NOT manually generate complete dependency paths. Only extract direct relationships. Complete downstream paths should be derived programmatically from the direct-edge graph.

---

## Step 1 — Inspect the HTML first

Before writing the full parser:

1. Inspect several exported HTML files/pages representing:

   * a Data Source
   * a Library Input
   * Dictionary
   * Computation
   * Projection
   * Output
   * Library-level UDF
   * Project-level calculation
   * Project-level UDF

2. Determine:

   * DOM structure
   * headings/classes/IDs
   * how tables are represented
   * how Name, Value, Expression, Type, Shape, etc. are represented
   * whether references use `<a href>` links
   * what the `href` contains
   * whether different PathWise object types use consistent HTML structures

3. Print a short summary of the structures you find before implementing extraction.

Do not invent CSS selectors before inspecting the actual HTML.

---

## Step 2 — Create a Node table

Create one record for each meaningful PathWise object.

Suggested schema:

`node_id`
`model`
`name`
`native_type`
`scope`
`parent_id`
`source_file`
`source_anchor`
`expression`
`data_type`

Possible `native_type` values include:

* MODEL
* DATA_SOURCE
* DATA_SOURCE_FIELD
* LIBRARY
* LIBRARY_INPUT
* DICTIONARY
* DICTIONARY_FIELD
* COMPUTATION
* COMPUTATION_FIELD
* PROJECTION
* PROJECTION_FIELD
* OUTPUT
* UDF
* PROJECT_CALCULATION
* CONSTANT
* UNKNOWN

Possible `scope` values:

* MODEL
* PROJECT
* LIBRARY
* DATA_SOURCE

Generate stable IDs that include enough hierarchy to avoid naming collisions.

Example concept:

`INT.LIB.InterestRate.HullWhite.PROJ.revised_short_rate`

Do not assume names are globally unique.

---

## Step 3 — Extract only DIRECT edges

Create an Edge table with:

`edge_id`
`from_id`
`relation`
`to_id`
`edge_category`
`source_file`
`source_expression`
`resolution_method`
`confidence`

Suggested relationship vocabulary:

### Structural

* CONTAINS
* HAS_INPUT
* HAS_OUTPUT
* HAS_CALCULATION
* HAS_UDF
* USES_LIBRARY

### Data lineage

* MAPS_TO
* INPUT_TO
* DERIVES
* UPDATES
* EXPOSES_AS
* FEEDS

### Execution

* CALLS
* CALLS_UDF
* DEPENDS_ON

### Control

* SELECTS
* ENABLES
* BRANCHES_TO

Do not create transitive relationships.

For example, if:

A → B
B → C

store only A→B and B→C.

Do NOT create A→C during extraction.

---

## Step 4 — Resolve references

Use this priority:

1. HTML hyperlink / anchor target if available
2. fully qualified expression reference
3. local PathWise scope resolution
4. unique-name lookup
5. unresolved reference

Do not silently guess when multiple candidates exist.

For unresolved or ambiguous references:

* create an unresolved-reference record
* retain the original expression text
* flag it for manual review

Suggested `resolution_method` values:

* HYPERLINK
* FULLY_QUALIFIED
* LOCAL_SCOPE
* UNIQUE_NAME
* UNRESOLVED

---

## Step 5 — Handle variable renaming / lineage

PathWise variable names may change between:

Data Source
→ Library Input
→ Computation
→ Projection
→ Output
→ another Library Input

Preserve these mappings as explicit direct edges rather than treating differently named variables as unrelated.

Example concept:

`DataSource.x`
→ MAPS_TO →
`Library.Input.y`

`Projection.z`
→ EXPOSES_AS →
`Library.Output.forward_rate`

Do not try to collapse all aliases into one variable yet. Preserve the original model objects and their direct mappings.

---

## Step 6 — Parse expressions

For each Value or Expression field:

* identify referenced PathWise variables/components
* identify UDF calls
* preserve the original expression
* generate one direct edge for every resolved reference

Be careful with:

* array indexing
* row groups
* `prev(...)`
* function calls
* indexed lookups
* conditional expressions
* local variables
* constants
* similarly named variables in different libraries

Do not treat numeric literals or language keywords as model nodes.

---

## Step 7 — Build the graph

Use either `networkx` or a simple adjacency-list implementation.

Implement functions such as:

`get_direct_dependencies(node_id)`

`get_direct_dependents(node_id)`

`get_downstream_impact(node_id, max_depth=None)`

`get_upstream_dependencies(node_id, max_depth=None)`

For downstream traversal, use BFS so the result includes dependency distance:

* hop 1
* hop 2
* hop 3
* etc.

Prevent infinite loops if the model contains cycles.

Return the actual direct edges used to reach each node.

---

## Step 8 — Outputs

Generate:

### `nodes.csv`

All parsed nodes.

### `edges.csv`

All direct resolved edges.

### `unresolved_references.csv`

References the parser cannot resolve safely.

### `impact.csv`

Generated only when the user specifies a starting node. Include:

`start_node`
`affected_node`
`hop_distance`
`predecessor`
`relation`

Also optionally save the graph as JSON.

---

## Step 9 — Validation report

After parsing, print:

* number of nodes
* number of edges
* edges by relationship type
* nodes by native type
* number of hyperlink-resolved references
* number of text-resolved references
* number of unresolved references
* duplicate/ambiguous names
* isolated nodes
* cycles detected

Show 10–20 sample edges for manual validation.

---

## Important design principles

This is a static dependency graph, not an exact runtime execution graph.

Do not claim that every reachable node is necessarily affected for every product or scenario because PathWise may contain conditional branches.

Call traversal results:

**Potential / Known Static Dependency Impact**

rather than exact runtime impact.

The parser should extract technical facts. It should NOT decide:

* whether a component is reusable
* whether two Internal and Reference model components are functionally equivalent
* business meaning of a calculation
* whether a change is appropriate

Those semantic judgments will later be added by Quant/SME review.

---

## First implementation scope

Do not try to parse the entire model immediately.

First validate the approach on ONE model library, preferably InterestCredit or another selected library, including:

* its Inputs
* Calculations
* Projection
* Outputs
* UDFs
* direct upstream references
* direct downstream references that can be resolved

Once this works, generalize the parser.

Please first inspect the actual exported HTML structure and tell me whether this approach is feasible. Then implement the smallest working version and explain any PathWise HTML structures that prevent reliable extraction.


##8/27
I already have a working Python parser that converts exported PathWise HTML documentation into:

- nodes.csv
- edges.csv
- unresolved_references.csv
- graph output

The parser works well on the PathWise Reference model and currently produces 0 unresolved references there.

However, on the Internal FIA model, it produces 7,000+ unresolved references.

I identified one important root cause:

Some Library Inputs directly reference Data Source fields without a `Lib.` or other library alias/prefix.

For example, the expression may directly reference something like:

`inforce.investment_account`

or

`market_data.xxx`

or another Data Source field.

These are valid PathWise Data Source references, but the current resolver sometimes fails to recognize them and incorrectly classifies them as:

- UNKNOWN nodes
- unresolved references

I need you to improve the parser's reference-resolution logic comprehensively.

## Important constraints

Do NOT execute the parser.

Do NOT rewrite unrelated parts of the parser.

Do NOT redesign the node/edge output schema unless absolutely necessary.

I need Python source code that I can manually merge into the existing parser.

If you do not have enough context about the existing code, write the improved resolver as a self-contained replacement module with clear integration instructions.

---

## 1. First define the reference-resolution problem

PathWise references may appear in several forms:

### A. Hyperlink-resolved references
The HTML may contain an `<a href>` or anchor pointing directly to the referenced object.

These should have the highest priority.

### B. Fully qualified Library references

Examples may conceptually look like:

`Lib.InterestRate.Output.forward_rate`

or another fully qualified library/object path.

### C. Relative references inside the current Library

Examples:

`Projection.factor`

`Dictionary.some_parameter`

`Computation.some_value`

These should resolve relative to the current library and calculation block.

### D. Direct Data Source references

Examples:

`inforce.investment_account`

`market_data.some_curve`

`product_info.product_code`

These may appear without any `Lib.` prefix.

They should resolve to existing DATA_SOURCE or DATA_SOURCE_FIELD nodes when the first namespace matches a known Data Source.

### E. Project-level Calculation references

Examples may refer to centralized project calculations such as:

`RateSetting.xxx`

`Cashflow.xxx`

or other project-level calculations.

These must be distinguishable from Libraries and Data Sources.

### F. Project-level UDF references

A function call may resolve to a project-level user-defined function.

### G. Library-level UDF references

A function may resolve only within the current library.

### H. Local variables / current calculation block variables

A symbol may refer to a locally defined variable within the same Dictionary, Computation, Projection, or calculation block.

### I. Built-in PathWise / language functions

Examples may include:

`prev`

`exp`

`min`

`max`

or other built-in functions.

These should NOT become UNKNOWN nodes or unresolved model references.

### J. Array/index references

Examples:

`foo[idx]`

`market_roll_forward[roll_forward_index[step]].swap_rate`

The resolver should identify the meaningful model objects without treating every index expression or numeric literal as a model dependency.

### K. Dynamic or ambiguous references

If the reference cannot be safely resolved using static information, preserve it as unresolved.

Do NOT guess.

---

## 2. Build explicit symbol indexes

The resolver should not rely on global string matching alone.

Build separate indexes for:

- Data Source names
- Data Source fields
- Libraries
- Library Inputs
- Dictionary variables
- Computation variables
- Projection variables
- Library Outputs
- Project-level Calculations
- Project-level UDFs
- Library-level UDFs
- Constants

For example:

`datasource_index`

`library_index`

`project_calculation_index`

`project_udf_index`

`library_udf_index`

`local_scope_index`

Also maintain a global name index only as a fallback.

Do not merge objects from different scopes merely because they have the same short name.

---

## 3. Use a deterministic resolution priority

Implement reference resolution using this order:

1. HTML hyperlink / anchor target
2. exact fully qualified node ID or fully qualified reference
3. same calculation-block local scope
4. same library scope
5. known Data Source namespace
6. project-level calculation
7. project-level UDF
8. library-level UDF
9. constants
10. unique global match
11. ambiguous / unresolved

This priority should be explicit in the code.

---

## 4. Fix direct Data Source resolution

This is the immediate bug I need addressed.

If the reference has the form:

`namespace.field`

and `namespace` matches a known PathWise Data Source, resolve it directly as:

`DATA_SOURCE_FIELD`

even if there is no Library prefix.

For example:

`inforce.investment_account`

should resolve to the existing node representing that Data Source field.

Do NOT create an UNKNOWN node if the Data Source and field already exist.

Also handle deeper Data Source references if the HTML structure supports them.

For example:

`market_data.curves.usd.xxx`

should preserve the correct hierarchy if that hierarchy exists in the parsed nodes.

---

## 5. Normalize references before resolution

Create a normalization step that:

- removes irrelevant whitespace
- separates array indexing from the referenced object
- identifies function-call syntax
- preserves dotted namespaces
- ignores numeric literals
- ignores language keywords
- ignores known built-in functions
- handles `prev(variable)` by resolving the underlying variable rather than treating `prev` as a model node

Do not over-normalize and lose scope information.

---

## 6. Do not create UNKNOWN nodes too early

The current parser appears to create UNKNOWN nodes for some unresolved references.

Change this behavior.

Only create a graph node when there is evidence that it represents a real PathWise object.

For unresolved references, store them in `unresolved_references.csv` instead of automatically adding UNKNOWN nodes to the canonical graph.

If UNKNOWN nodes are still needed for compatibility, clearly separate:

- real parsed PathWise nodes
- synthetic unresolved placeholders

Prefer not to include synthetic placeholders in the main dependency graph.

---

## 7. Improve unresolved-reference diagnostics

For every unresolved reference, output:

`raw_reference`

`normalized_reference`

`source_node_id`

`source_scope`

`source_library`

`source_block_type`

`source_expression`

`unresolved_category`

`candidate_matches`

`resolution_attempts`

`reason`

Suggested unresolved categories:

- AMBIGUOUS
- DYNAMIC_LOOKUP
- UNKNOWN_NAMESPACE
- MISSING_NODE
- UNSUPPORTED_SYNTAX
- LOCAL_SCOPE_FAILURE
- DATASOURCE_RESOLUTION_FAILURE
- PROJECT_SCOPE_FAILURE
- UDF_RESOLUTION_FAILURE
- OTHER

This will allow us to analyze root causes instead of manually reviewing thousands of records.

---

## 8. Return resolution metadata for every successful edge

For every resolved dependency, include:

`resolution_method`

Possible values:

- HYPERLINK
- FULLY_QUALIFIED
- LOCAL_BLOCK
- LOCAL_LIBRARY
- DATA_SOURCE
- PROJECT_CALCULATION
- PROJECT_UDF
- LIBRARY_UDF
- CONSTANT
- UNIQUE_GLOBAL

Also include:

`confidence`

Suggested rule:

- HIGH: hyperlink or exact fully qualified reference
- HIGH: exact Data Source namespace + field match
- MEDIUM: deterministic local-scope resolution
- LOW: unique global fallback

Do not silently assign HIGH confidence to fallback matching.

---

## 9. Preserve edge direction

The graph should represent data/dependency direction consistently.

If variable A is referenced in the expression defining B:

`B = f(A)`

the dependency edge should be:

`A -> B`

not:

`B -> A`

For a Library Input:

source variable -> library input

For an Output mapping:

internal calculation/projection variable -> library output

For a UDF call:

caller/calculation -> UDF may be stored as a CALLS_UDF execution edge, while data arguments should still generate their own dependency edges.

---

## 10. Avoid duplicate edges

If the same direct dependency is found multiple times, store one canonical edge and optionally include:

`occurrence_count`

and/or

`evidence_locations`

Do not create thousands of duplicate edges for the same relationship.

---

## 11. Add diagnostic comparison tools

Please also provide functions that can report:

- unresolved references by source object type
- unresolved references by namespace
- unresolved references by root pattern
- unresolved references by library
- unresolved references by project calculation
- resolution methods by count
- top 50 unresolved patterns

I need to compare the Reference model and Internal model to understand why one resolves fully and the other does not.

---

## 12. Validation before full-model rerun

Do not assume the new resolver is correct simply because unresolved count decreases.

Provide a validation workflow for a selected library, such as InterestCredit:

1. manually specify 20 known relationships
2. compare expected vs extracted edges
3. report:
   - correct edges
   - missing edges
   - incorrect edges
   - unresolved expected relationships

Precision is more important than forcing unresolved count to zero.

Do not guess references merely to reduce the unresolved count.

---

## 13. Deliverable

Please provide:

1. A short explanation of the root cause and proposed resolver architecture
2. A self-contained Python implementation of the improved reference-resolution module
3. Any helper classes/functions required
4. Exact instructions showing where the new resolver should be called from the existing parser
5. Any minimal changes required to the existing node/edge extraction flow
6. Example unit tests for:
   - direct Data Source reference
   - same-library relative reference
   - project-level calculation
   - project UDF
   - library UDF
   - built-in function
   - array/index reference
   - ambiguous reference
7. A diagnostic function for unresolved-reference root-cause analysis

Do NOT execute the code.

Do NOT regenerate the entire parser unless absolutely necessary.

The goal is to improve reference resolution while preserving the existing parser architecture and output format.

8/27 V2
I want you to rewrite the PathWise HTML dependency parser from scratch as a cleaner V2 implementation.

The current parser works well for the Reference model but creates 7,000+ unresolved references for the Internal FIA model. After reviewing the failures manually, the main problem is that the current resolver relies too much on global name matching and does not fully understand PathWise scope, namespaces, local variables, built-in functions, control flow, or variable aliasing.

Please use the actual exported PathWise HTML structure you already inspected.

## Goal

Create a complete Python script that converts exported PathWise HTML documentation into:

- `nodes.csv`
- `edges.csv`
- `unresolved_references.csv`
- `graph.json`

The parser should extract only **direct, evidence-backed relationships**.

Do not create transitive dependency edges during parsing.

---

# 1. Clean expressions before parsing

Preserve both:

- `raw_expression`
- `clean_expression`

Before extracting references, remove comments from `clean_expression`:

- block comments: `/* ... */`
- line comments: `// ...`

Do not extract dependencies from comments.

---

# 2. Build normalized PathWise nodes

Parse model objects such as:

- Data Sources
- Data Source fields
- Library Groups
- Libraries
- Library Inputs
- Dictionary variables
- Computation variables
- Projection variables
- Library Outputs
- Project-level Calculations
- Project-level UDFs
- Library-level UDFs
- Constants

Keep the original HTML/native path for traceability.

Also create a shorter normalized `node_id`.

Example:

Native path:

`MODEL.Test0.DATA_SOURCE.Data_Sources.DATA_SOURCE.market_data.DATA_SET.market_roll_forward`

Normalized node ID:

`Test0.DS.market_data.market_roll_forward`

For libraries:

`Test0.GRP.InterestRate.LIB.HullWhite.COMP.market_libor_spot`

Keep `GROUP/GRP` because a PathWise Library Group and an individual Library are different structural levels.

---

# 3. Build explicit symbol indexes

Create separate indexes for:

- Data Source names
- Data Source fields
- Library Groups
- Libraries
- Library Inputs
- Dictionary variables
- Computation variables
- Projection variables
- Library Outputs
- Project-level Calculations
- Project-level UDFs
- Library-level UDFs
- Constants

Do not rely mainly on one global short-name index.

Objects with the same short name in different scopes must remain separate.

---

# 4. Parse expressions structurally

Do not simply tokenize names and search globally.

Recognize:

- fully qualified references
- relative references
- local variables
- function calls
- array/index access
- attribute access
- control-flow syntax
- literals
- built-in functions
- UDFs

Separate function resolution from argument resolution.

If a function itself cannot be resolved, its arguments should still be parsed for dependencies.

---

# 5. Use scope-aware reference resolution

Use this priority:

1. explicit/direct reference from HTML metadata
2. exact fully qualified path
3. current calculation-block local scope
4. current Library scope
5. current Library Inputs / Outputs / UDFs
6. project-level Calculations / UDFs
7. top-level Data Sources
8. other Library Outputs only, if PathWise allows that interface
9. unresolved

Do NOT search arbitrary fields inside unrelated Libraries.

Do NOT use global short-name matching as the normal resolution method.

If global matching is retained at all, it must be an optional low-confidence fallback and must never create an edge automatically when multiple candidates exist.

---

# 6. Resolve qualified Data Source references correctly

References such as:

`market_shocks.libor_spot_proxy`

`market_base.libor_spot_proxy`

`market_roll_forward[...].libor_spot_proxy`

must preserve their namespace.

Do not reduce all of them to:

`libor_spot_proxy`

Resolve the longest qualified path first.

For example:

`market_shocks.libor_spot_proxy`

should resolve specifically to the `market_shocks` Data Source/Data Set field.

It should not become AMBIGUOUS merely because another Data Source also contains a field named `libor_spot_proxy`.

---

# 7. Built-in functions are not graph nodes

Maintain a configurable registry of PathWise/system built-ins.

Examples may include:

- `last`
- `prev`
- `exp`
- `sqrt`
- `min`
- `max`
- `ceil`
- `iif`
- `sum_range`
- `intg_exp`
- date/calendar functions
- RNG/system functions

Built-in functions should:

- not become nodes
- not become unresolved UDFs
- not block parsing of their arguments

Example:

`last(cashflow.interest_credit_percentage_first)`

should generate a dependency from:

`cashflow.interest_credit_percentage_first`

to the target variable.

Optionally record:

`reference_modifier = LAST`

Similarly:

`prev(x)`

should preserve the dependency on `x`.

---

# 8. Handle local temporary variables

Expressions may define local temporary variables inside one calculation block.

Example:

`anni_idx = ceil(years_since_issue + 1e-6)`

then later:

`withdrawal_rate_annual[0, anni_idx]`

The parser should maintain a local symbol table for the current expression/calculation block.

Do not search the whole model for `anni_idx`.

For the POC, local temporary variables may be collapsed so the final graph connects the real upstream dependencies directly to the final PathWise calculation node.

Example:

`years_since_issue`
→ final calculation

and

`withdrawal_rate_annual`
→ final calculation

Local temporary variables do not need to become permanent global graph nodes unless useful for debugging.

---

# 9. Dynamic index is not necessarily dynamic target

Do not classify every indexed expression as `DYNAMIC_LOOKUP`.

Example:

`withdrawal_rate_annual[0, anni_idx]`

has a known target:

`withdrawal_rate_annual`

Only the index is dynamic.

The parser should still resolve the dependency on `withdrawal_rate_annual`.

Use `TRUE_DYNAMIC_LOOKUP` only when the actual target model object is determined dynamically at runtime, such as string-driven/object-driven lookup where the static target cannot be known safely.

---

# 10. Parse switch/case and control flow

Recognize PathWise control-flow syntax such as:

- `if / elseif / else`
- `switch / case`
- `return`

These keywords are not model nodes.

Example:

`switch inforce.investment_account`

should create a control dependency from:

`inforce.investment_account`

to the target calculation.

Variables referenced inside each `case` should also generate normal data dependencies.

Do not classify `switch`, `case`, or branch keywords as `MISSING_NODE`.

Optional edge metadata may include branch/condition information, but this is not required for the POC.

---

# 11. Handle structured attribute access

References such as:

`run.eop_date.year`

`run.eop_date.month`

may represent a structured object with attribute access.

Resolve the meaningful base model object first, for example:

`run.eop_date`

and preserve `.year` / `.month` as attribute metadata if needed.

Do not automatically treat every dotted suffix as a separate namespace.

---

# 12. Direct edges only

If B is defined using A:

`B = f(A)`

create:

`A -> B`

For Library Inputs:

`upstream source -> Library Input`

For Output mappings:

`internal variable -> Library Output`

For UDF calls:

create a `CALLS_UDF` execution edge if useful.

Also separately extract dependencies from UDF arguments.

Do not create transitive edges such as A→C when the actual direct structure is:

A→B  
B→C

---

# 13. Recommended relation types

Use a controlled vocabulary.

Structural:

- `CONTAINS`
- `HAS_INPUT`
- `HAS_OUTPUT`
- `HAS_CALCULATION`
- `HAS_UDF`
- `USES_LIBRARY`

Data lineage:

- `MAPS_TO`
- `INPUT_TO`
- `DERIVES`
- `UPDATES`
- `EXPOSES_AS`
- `FEEDS`

Execution:

- `CALLS`
- `CALLS_UDF`
- `DEPENDS_ON`

Control:

- `SELECTS`
- `CONTROLS`
- `ENABLES`
- `BRANCHES_TO`

Do not invent new relation names unless necessary.

---

# 14. Avoid synthetic UNKNOWN nodes

Do not automatically create UNKNOWN graph nodes for unresolved tokens.

Only create canonical graph nodes when the HTML/model documentation provides evidence that the object exists.

Unresolved references should go to `unresolved_references.csv`.

---

# 15. Improve unresolved classifications

Use these categories:

- `AMBIGUOUS_SCOPE`
- `TRUE_DYNAMIC_LOOKUP`
- `UNKNOWN_NAMESPACE`
- `MISSING_PARSED_NODE`
- `UNSUPPORTED_SYNTAX`
- `LOCAL_SYMBOL_FAILURE`
- `DATASOURCE_RESOLUTION_FAILURE`
- `PROJECT_SCOPE_FAILURE`
- `UDF_RESOLUTION_FAILURE`
- `BUILTIN_NOT_REGISTERED`
- `CONTROL_FLOW_NOT_PARSED`
- `ATTRIBUTE_ACCESS_FAILURE`
- `OTHER`

For every unresolved reference include:

- raw reference
- normalized reference
- source node
- source Library
- source block type
- raw expression
- clean expression
- category
- candidate IDs
- resolution attempts
- detailed reason

---

# 16. Resolution metadata

For successful edges include:

- `resolution_method`
- `confidence`
- `source_file`
- `source_anchor`
- `raw_expression`
- `clean_expression`

Suggested resolution methods:

- `DIRECT_REFERENCE`
- `FULLY_QUALIFIED`
- `LOCAL_BLOCK`
- `LOCAL_LIBRARY`
- `DATA_SOURCE`
- `PROJECT_CALCULATION`
- `PROJECT_UDF`
- `LIBRARY_UDF`
- `CONSTANT`

Avoid calling the method `HYPERLINK`; use `DIRECT_REFERENCE` for explicit machine-resolvable references contained in the documentation.

---

# 17. Deduplicate edges

The same dependency may appear multiple times.

Store one canonical direct edge and optionally include:

- `occurrence_count`
- evidence locations

Do not create many duplicate graph edges.

---

# 18. Diagnostics

After parsing, print:

- total nodes
- total direct edges
- unresolved count
- unresolved count by category
- unresolved count by Library
- unresolved count by namespace
- edge count by relation type
- resolution count by method
- top unresolved patterns
- ambiguous candidate patterns

This is important for comparing the Reference model and Internal FIA model.

---

# 19. Validation

Do not optimize for unresolved count alone.

Precision is more important than forcing everything to resolve.

The parser should prefer:

`unresolved`

over creating a wrong dependency.

Provide a simple validation helper where I can define manually verified edges for a Library such as InterestCredit and compare:

- expected edges
- correctly extracted edges
- missing edges
- incorrect edges

---

# 20. Code requirements

Write one complete replacement Python implementation.

The code should be modular and readable.

Add very short comments before each major step only, for example:

```python
# Remove comments before parsing expressions.
```

```python
# Build scope-aware symbol indexes.
```

```python
# Resolve qualified references before local names.
```

```python
# Parse built-in calls but keep argument dependencies.
```

Do NOT add long explanatory comments throughout the code.

Prefer small functions with clear names instead.

Do not execute the code.

At the end provide:

1. required Python packages
2. example command to run the parser
3. short explanation of the main modules/functions
4. known limitations
5. where the built-in function registry can be extended

The goal of V2 is not to force the Internal FIA model to zero unresolved references. The goal is to correctly resolve all statically resolvable PathWise relationships while clearly classifying genuinely ambiguous, dynamic, or unsupported references.
